# Roadmap

> **Beta.** Forward todo in execution order; this file absorbs the per-milestone specs, and `git log` holds their full prose. The two control-plane YAMLs + the ADRs are the only other live contracts.
>
> **Live now:** deployed at `https://app.promptpotter.com` (Cloudflare Tunnel + systemd, OIDC — see [`deploy-linux/`](../../deploy-linux/README.md)). **Open signup**: completing OIDC grants access, bounded by a per-account lifetime spend ceiling rather than an approval queue, on **one shared LLM key** from `.env` — so the sequence below is "harden a thing already serving users," not "prep before launch."
>
> **Three ways to run it, by who operates it.** *We* run it (the hosted beta above, limited free then BYO key — Lane A2, spec-only today) · *you* run it (local, Claude-operated via `/potter-run` on your own keys — [`../manual/02-install.md`](../manual/02-install.md)) · *your team* runs it (the same self-hosted stack as the beta, multi-user + whitelabel). The developers and operator run tier 3. Tiers 1 and 3 are one codebase plus the `deploy-linux/` stack, differentiated by who owns the box, not by a fork.

## Hard ordering (violate → rebuild)

- **Build every new webapp data panel on `dashboard.json` polling + the SSE ledger-tail.** That pair is the design, not an interim seam awaiting a cutover; there is no `live-state` endpoint to wait for. **One exemption, and its shape is the rule: a ONE-SHOT TOPOLOGY read may sit beside the poll when the poll is its INVALIDATION signal rather than its transport.** `GET /campaigns/{id}/pipeline` is the only PANEL-feeding one — a pipeline resolution cannot change without a new cycle or a new searchpoint, and `dashboard.json` announces both. A panel whose content changes *while nothing else does* fails that test and rides the poll. `POST /campaigns/{id}/fork-preview` (`manifests.py::preview_fork_steer`) is a read too, but it sits OUTSIDE this rule rather than under it: it is keyed on the operator's pending gesture, which no poll can invalidate because the run does not know about it. Feeding a panel is what puts a read under the rule.
- **BYO per-user API keys — now the load-bearing half, and unbuilt.** Signup is open and each account is metered against `FREE_TIER_SPEND_CAP_USD` + `FREE_TIER_TOKEN_CAP` (lifetime, `quota.py::lifetime_ceilings`), so the host key is bounded — but a user who spends their ceiling has **nowhere to go**. That is the liability now, not the unbounded spend it replaced. Lane A2.
- **HTTP-edge abuse protection is now due.** Cloudflare edge + the per-account ceiling + per-user `JobRegistry` quotas bound the public surface; with the approval queue gone, app-level rate-limiting (C6) is the remaining gap — nothing bounds the NUMBER of accounts, only what each one may spend.

Any new endpoint is multi-tenant by default.

## Lane 0 — daily hygiene

Drain before feature work: [`code-debt-cleanup`](code-debt-cleanup.md).

## Sequence

Sequenced into lanes by dependency, not milestone number. **Front priority = Lane A + the bench lane, concurrent** (no shared seam); the publication lane is now downstream of the bench rather than beside it. Lane B is closed; Lane C follows A.

### Lane A — beta usable for free-tier web users, end-to-end

| # | Item | Status |
|---|---|---|
| A1 | First-run exhibit — a finished campaign a new account reads before supplying anything | **no working dataset** — `screen-taste-v0` is a stub whose origin read below its own chance floor; cutting a replacement is its own work-scope after the release. The mechanism that SERVES it (install-tier campaign root, `demo_mode_enabled` reader) is unbuilt — [`recommender-demo.md`](recommender-demo.md) |
| A2 | Host coupon + BYO per-user API keys | pending — **overdue** (see § Host coupon + BYO per-user API keys); token HQ at `/auth/{quota-status,activity}` already shipped |
| A3 | Anonymous preview tier — the public site's chat before sign-in | spec only, deliberately (see § Anonymous preview tier); blocked on the chat backend existing |

### Lane C — product differentiator + capability (after A)

| # | Item | Status |
|---|---|---|
| C1 | **Chat-first front door** — one thread: ingest/check-in → curated activity stream → inline decision buttons (existing verbs). | Arc 2 (conversation endpoint) deferred — [`chat-foundation.md`](chat-foundation.md) |
| C2 | Composite fitness P2–P4 (P1 = spend, done) — data rollup anytime; **scatter panel after P3** | pending (see § Connectors + L4) |
| C2b | **Judged + turn-structured scoring** — an LLM-as-judge as a measured observation, and the per-step ruler it opens | judges, the grading call path and the `retrieve → ground → answer` schema all SHIPPED (`promptpotter/judges/`); open: the per-step ruler (see § Judged and turn-structured scoring) |
| C3 | L4 closure — the recursion + the L4 campaign + `proxy_lift_corr ≥ 0.6` re-validation | Open — the roster is [`l4-outer-loop.md`](l4-outer-loop.md) § Open, read there and never subsetted here |
| C4 | Cross-user measurement panel (after P3) | pending (see § Ingest + chat-first web) |
| C5 | MCP server mode (= **agent-tool parity**, see § Agent-tool parity) · user-editable `pipeline.yaml` in UI · **the connector as a unit** (see § The connector as a unit) | pending — the connector unit is a draft design, and its first dependency, Harbor's second skill-delivery mode, shipped; the editable half's gate is LIFTED: the served campaign pipeline resolution shipped (`architecture.md` § Two resolution seams), so the surface can now READ the value it would write back, and what remains is the write side alone |
| C6 | Public-service hardening (Docker, metrics, rate-limit, billing) — `/health` shipped; **pull rate-limit/metrics forward if the beta opens past the allowlist** | pending |
| C7 | Non-prompt targets + evolutionary operators · **agent harnesses** (§ Evolving agent harnesses) · multimodal · research extensions | pending — after v1, except the agent-harness half, pulled forward and part-shipped (§ Evolving agent harnesses) |
| C8 | **Mask abstraction** — backend organizing structure (alternative-criterion + transferability); M1 = scoring-function-swap divergence + minimal visual clues, then migrate every divergence trigger onto it | M1 + abort + the scoring write side shipped (see § Lineage mask) |

**Parallel lane — the bench = M13, and it is the project's closing focus**. A third party's search method plugs in as the round's candidate source and inherits everything downstream; the design is § The optimizer plug point, the running record `.scratch/m13-bench.md`. **It closes when ONE peer runs end-to-end in a real campaign** — a head-to-head whose only difference is the proposer. The arm roster starts at our own `l1_generate`, the DSPy family (GEPA / MIPROv2 / BootstrapFewShot) and CAPO; the WikiSkill / SkillOpt *setting* is already wired as Harbor's advanced-vs-primitive skill delivery ([`../operations/dataset-reasoning-matrix.md`](../operations/dataset-reasoning-matrix.md)), so "PP+WikiSkill vs WikiSkill" is a grid CELL rather than a new surface — `evidence --grid ROW,COL` discovers `proposer` as a factor off each subject's own config exactly as it does `response_format`. SIFT joins the roster as a whole configuration rather than a proposer alone — § SIFT reproduction.

**Downstream lane — publication = M14, and the bench IS its contribution.** Dependency sequence, no dates, running record in `.scratch/m14-preprint.md`; manuscript in `paper/`. BBEH primary, AIME in band, HotPotQA queued but unwired, GSM8K's saturation verdict withdrawn and now a pilot candidate — roster and the admission bar in [`../research/benchmarks.md`](../research/benchmarks.md); 3 seeds; paired per-cell differences with clustered SEs; exact/bootstrap CIs; Holm/BH across the proposer matrix ([`../research/external-constraints.md`](../research/external-constraints.md) § M14) vs CAPO/DSPy; ablation rows L1 / L1+L2 / full · scan · cross-run archive · critique · zero-signal-filter. Competitor + L4 numbers wait on C3. Endpoint hardening P0 (auth dep on every router, pinned `ALLOWED_ORIGINS`, `extra=forbid` on request models, poll rate-limit) lands before any non-localhost open.

**The four publication blockers re-read against the bench — three DISSOLVE by construction, not by repair**, and all four are stated in [`../research/bbeh-comparison/README.md`](../research/bbeh-comparison/README.md). *Grader*: `matchers.py::_label_match` runs `extract_last_bold` on both sides while the notebooks inline a whole-string compare, and on a chain-of-thought benchmark that difference favours us — a peer arm measured through `measure_sample` → `rescore_results` → the campaign's compiled formula makes the notebook code path cease to exist. Worse than previously written: both notebooks `from shared_config import exact_match`, which that directory's `shared_config.py` does not define, so the two copies have already drifted — which is precisely what the byte-equivalence rule existed to prevent. *Model*: an arm is measured through the dataset's resolved `pipeline_params` and `configure_and_apply_pipeline` RAISES on a node with no `config.model` rather than defaulting, so a peer cannot reach a different one without editing the file both sides read. *Split*: `select_round_subset` runs BEFORE generation, so no proposer picks its own exam. **What survives** is the optimization budget, converted from unmeasurable to one meter plus a decision: every arm's spend lands in one `SpendRollup`, and holding calls constant *and* reporting spend as a dimension beside them — what reviewers ask for first ([`../research/external-constraints.md`](../research/external-constraints.md) § Ranked, item 4) — is then a knob and a paid run. The 28% BBEH-mini reading dies by re-measurement regardless; no orchestration re-derives a number taken at a model the dataset no longer pins.

**Two artifacts the wrap CREATES, both ours to declare.** A peer never shown a panel scores 0% on `evidence_grounding_present` and the `*_behavior` scorers, so those are N/A for a non-default proposer or `review.md` publishes an artifact as a result. And the wrap is itself a confound a reviewer will name — GEPA-inside-PromptPotter is not GEPA. The honest framing is the milestone's own: this measures **a peer's proposals under a held-constant harness**, a better-posed question than end-to-end. So **do not delete the notebooks** — they become the uncontrolled reference arm that shows what the control was worth.

**Far-horizon (unscheduled).** A synthetic dataset cut from one hold-out question, which would remove the dataset-provision requirement — the real metric there is synthetic→real transfer of *optimizer lift*, anchored on the single genuine hold-out · an AlphaEvolve code-harness · dogfooding against a self-hosted gateway, which would prove the "gateway routes, PromptPotter tunes what it routes to" pairing against a real system and exercise the REST surface as an external caller, forcing the inbound credential we do not have. Opportunistic, no date.

## Permanent contracts (constitutions, not steps)

**The ADR roster is the [`../adr/`](../adr/) directory listing, never an enumeration** — a subset here is what leaves one unindexed ([`../CLAUDE.md`](../CLAUDE.md) § What may live in `specs/`). Two live contracts are not ADRs, so they are named here:

- **Frontend surface** — per-control behavior per auth/data state. → [`frontend-surface-contract`](frontend-surface-contract.md)
- **Verdict resolution** — the statistical model behind the live adaptive queue + `hard_samples.json`. → [`verdict-resolution`](../methods/verdict-resolution.md)

---

## Design notes (folded specs)

Terse landing for the per-milestone specs consolidated here. Status is truth; the original prose is in `git log`.

### The optimizer plug point — PromptPotter as a bench

**One seam, and it is a single call.** `l1/candidate_source.py::generate_or_load_candidates` is the only place a round's arms are minted; everything in it *except* the `l1_generate(...)` call is the contract around minting — the replay branch keyed on `round_document_digest` (what makes resume and fork correct), `detect_invariants`, the `CandidateMintedRecord` append, both `L1_GENERATE` phase emits, the synthesized `LLMCallRecord` on replay. A plugin rides all of it unchanged; only that one call becomes a dispatch, with `l1_generate` as the built-in member.

**A plugged-in proposer is HANDED a frozen `domain/` request and never `Cycle`** — the house precedent is a connector getting `InProcessWorkload` and never the session. The request carries `parent`, the **already resolved** `n_variants`/`creativity` (re-deriving them breaks the round budget), `round_num`, the open axes, the measured `rounds`, and a read-only view of the round's `scoring_set`. It returns `(list[CandidateProposal], reason | None)` — zero arms *with a reason*, because `l1_parse_failure` routes the heal and drops the round from L4 outer scoring. Scoring, the round loop and `OptSearchPoint` are untouched, and **that absence is the whole value**: a peer's arms go through `PARAM_FORBIDDEN_KEYS`, `validate_overrides`, the node's permitted model set, PoBB, the δ ruler and `elect_round_winner` exactly as ours do. `IndividualLineage.source` is a free `str`, so `source="optimizer:gepa"` needs no enum edit.

**Three conditions keep this off the L4 ban** ([`optimization/CLAUDE.md`](../../promptpotter/application/optimization/CLAUDE.md) § Add no 4th LayerStrategy). `LayerStrategy.layer_id` is `Literal["L2","L3"]` with no L1 member, so a plugin takes L1's *seat*, below the ladder — a swap at rung one, not a fourth rung. But: (a) **no escalation channel reaches a plugin** — a peer's own reflection runs inside its proposal call, the way L4 expresses nesting as re-entrancy rather than as a new rung; (b) **a non-default proposer forces `escalation_ladder: l1` and stands `ensure_prior_critique` down**, declared as a `knobs.py::COUPLINGS` row rather than a preflight branch, because L2 writes only `l1_layout` and `l1_overrides` — surfaces a plugin does not read — so every L2 fire would be a paid no-op that `l2_targets_l1_surface` scores as *conformant*, which is exactly the failure the deleted probe-round lever produced; and `ensure_prior_critique` is a whole paid `l1_critique` call, billing our algorithm to the peer. (c) The naming ban extends by the same derivation: no `optimization/l1/gepa.py` — ours lives beside `l1/generate.py`, a third party's lives in their package.

**Registration is a THIRD entry-point group, and the third copy is what forces the fold.** `promptpotter.optimizers` beside `promptpotter.connectors` and `promptpotter.judges` — not merged into connectors, whose `_validate` checks `wire_adapter`/`extract_experiment`/`session_factory` and means nothing on a proposer, and where `get("dspy")` would become ambiguous against the `dspy` connector pointing the opposite way. But `connectors/__init__.py::_load/_validate/_add/connector_origins` and its judges twin are the same ~40 lines, so a third hand-copy is the shape `<root-fix>` refuses: one stdlib-only helper `(group, builtins, validate) → (table, origins)`, all three callers moved onto it in the same change. Selection is **one field** — `OptimizationConfig.proposer`, beside `optimizer_set` — so all five entry points get it for free and none needs a flag. It must NOT live in `assets/optimizer/pipeline.yaml`: that file is install-global and operator-shadowable, so a head-to-head on one box could not run two proposers at once. A served roster goes into `api-openapi.yaml` before the handler and serves the plugin's ORIGIN the way `connector_origins()` does — a plugin name greps to nothing in this tree.

**The proposer is OUT of measurement identity in a normal campaign, and IN it for an L4 inner cycle.** `pipeline_resolve.py::_identity_contributions` folds the connector's `identity_config` and the judges, not the proposer — which is what makes an arm a peer proposes and we already measured replay free, and what makes an N-way head-to-head affordable at all. But in the recursion the optimizer IS the instrument, so `connectors/promptpotter.py::_identity_config` must fold the plugin's name and version into the digest or two proposers' inner cells pool under one key. Same field, opposite answers, decided by which side of the recursion answers — **unstated, this is found by silent pooling**, never by an error.

**One thing not on the seam today: spend.** Peers evaluate internally (MIPROv2 minibatch trials, GEPA reflective rollouts), and those sends must land on the run's meter or the budget claim is void. The repo solved this once for `connectors/dspy_module.py`, and `Connector.holds_own_sends` is the declared vocabulary for "this thing bills where it runs" — a proposer needs the same declaration.

**Not every peer is a candidate source.** Opik's shape is a whole-loop optimizer — `optimize_prompt` with its own metric and `evaluate_prompt` ([`../research/external-constraints.md`](../research/external-constraints.md) § M13; Opik's own [extending-optimizers docs](https://www.comet.com/docs/opik/agent_optimization/)) — not a round's proposer. Reaching it is the outbound direction (a `BaseOptimizer` subclass in `promptpotteropt`), or an adapter that strips its evaluation and keeps only its proposals. And the seam must not assume L1 → L2 → L3 is the only shape a proposer runs in ([`../research/external-constraints.md`](../research/external-constraints.md) § L4): [EvoAgentX](https://arxiv.org/abs/2507.03616) plugs AFlow, TextGrad and MIPRO in as swappable evolution algorithms apart from the workflow topology, which is this seam seen from the other side. Whether a proposer should itself be an agent stays empirical — [AFlow](https://arxiv.org/abs/2410.10762) beat hand-designed workflows by 5.7% searching them as code graphs, while [AI Scientist v2](https://github.com/SakanaAI/AI-Scientist-v2), the more agentic version, did not reliably beat the templated v1 — so stage 4 is where that gets answered, not argued.

**Order.** Each stage closes on a verification, never a date.

1. **Declare the seam** (contract only): the frozen request, the return, and what a proposer may not touch — the exam, the ladder, the grader, the spend path. Recipe in [`../developer/adding-a-surface.md`](../developer/adding-a-surface.md), the stable half in [`../developer/stable-api.md`](../developer/stable-api.md). *Closes when* `l1_generate`'s signature maps onto the protocol with no remainder on either side; a remainder means the seam is in the wrong place.
2. **Fold the registry, then add the group.** *Closes when* the gate is green, all three origin maps answer through one function, and a `proposer: ""` campaign writes a byte-identical `round_NNNN.json` — that equality is what proves the seam inert.
3. **One real plug — the DSPy family**, behind the existing `[dspy]` extra, never core ([`ADR-0006`](../adr/0006-embeddable-core-and-extras.md)). Right first plug because both hard halves exist here already: `presentation/teleprompter.py` carries a `dspy.Module`, and `connectors/dspy_module.py` solved metering a third party's sends and the keep-the-caller's-LM trap. DSPy's interface is read: `Teleprompter.compile(student, *, trainset, teacher=None, valset=None)`, with GEPA's budget set by `max_metric_calls` ([`../research/external-constraints.md`](../research/external-constraints.md) § M13). *Closes when* a MIPROv2 round renders through `review_md.py` with no special case and its spend appears in the same `SpendRollup` as an `l1_generate` round.
4. **The comparability run** (PAID): BBEH, one model, one grader by construction, one split, arms = `proposer ∈ {ours, gepa, mipro, bootstrap}` at `escalation_ladder: l1`, total calls held equal across arms, 3 seeds; paired per-cell differences with clustered SEs; exact/bootstrap CIs; Holm/BH across the proposer matrix ([`../research/external-constraints.md`](../research/external-constraints.md) § M14). Retires the 28%. `<one-budget>` binds both axes — price it in dollars *and* wall clock before asking. **It yields TWO readings and a claim may quote only the matching one.** The *controlled* reading is a PROPOSER contrast: the ladder is off on every arm, so our arm is `l1_generate` alone and **not PromptPotter** — it answers "whose proposals are better under one harness", which is the better-posed question and the one the guard licenses. The *uncontrolled* reading is the SYSTEM comparison — full PromptPotter with the ladder on, against each peer run its own way — which is what a "PromptPotter vs GEPA" sentence needs and which the notebooks already produce. Neither substitutes for the other, and the forced `escalation_ladder: l1` is the price of the first, not a detail of it.
5. **The budget dimension** (PAID, strictly after 4): the same arms at two or three budget points, so cost is an axis rather than a constant. Stage 4 already matches calls at one point; this is the budget-matched Pareto comparison reviewers ask for first ([`../research/external-constraints.md`](../research/external-constraints.md) § Ranked, item 4), and what makes the claim distinctive. The reason it is asked first: [LEVI](https://arxiv.org/abs/2605.09764) matches or exceeds [GEPA](https://arxiv.org/abs/2507.19457) at less than half its rollout budget, so an optimizer's margin can live entirely in its budget, and [HAL](https://arxiv.org/abs/2510.11977)'s cost–accuracy frontier is the form reviewers will read it in.
6. **Third-party reachability**: a minimal example package — `pyproject.toml` entry point, a short proposer, the one-liner that proves it loaded. *Closes when* `pip install -e .` in a scratch venv plus `proposer: example` runs a round whose arms carry `source="optimizer:example"`, with **zero edits inside this tree**. That is the literal statement of the milestone.

**No new dependency anywhere** — the registry is `importlib.metadata`, the DSPy plug rides an existing extra. The one thing that would force core growth is the request object: keep it in `domain/` and pure. The moment it carries a store handle or a live client, both the core-is-the-engine boundary and the trusted-code story move — that is the design question to refuse, not to solve.

### SIFT reproduction — their method as a PromptPotter configuration

**SIFT-exact is a config, and the paper is the ablation ladder from it.** [SIFT](../research/landscape.md#sift--self-improvement-via-fast-tree-search-paper) (the Darwin Gödel Machine line, with a judge-ranked evaluation front end) is reproduced by turning PromptPotter's own mechanisms off and plugging SIFT's parts into the same seams. The reading is then one PromptPotter member swapped back in per arm — PoBB for the fixed gate, Rasch θ rank for accuracy rank, the hard-first order for the fixed subset, the held-out adoption gate — each paired against SIFT-exact at one budget and one seed set, so every claim reads "X instead of theirs buys Y at equal cost". The self-referential half (the improved optimizer proposes the next round) is [`l4-outer-loop.md`](l4-outer-loop.md) § Open, self-adopt mode.

**Turned off, all existing knobs:** `mechanisms.elimination.epsilon_elimination: false` and `mechanisms.elimination.leader_lock_in: false` (every candidate runs its budget) · `mechanisms.selection.per_round_resubset: false` plus its sibling mechanism (the sample basis frozen: one subset, one order) · `escalation_ladder: l1`, `rebase_capability: false`, `terminate_capability: false` · `n_variants: 1`, `max_rounds` at their step count · `sp_budget_round` at their search-subset size, the full benchmark as the held-out `dataset_split`.

**To build — five gaps, each a pluggable member with a SIFT implementation and ours**, so reproduction is selection, not a fork of the loop:

1. **Code-artifact candidates.** SIFT patches the agent's source and tools; a candidate here is prompt fields and pipeline params. The new candidate is a file set or patch, content-hashed into measurement identity, loaded per episode by an agent runner (Harbor or something lighter — S3 below decides) — an extension of § Evolving agent harnesses, and the one design-bearing gap.
2. **A self-proposer on § The optimizer plug point** — the parent agent run on its own source and evaluation logs, returning one patch. The plug point hands a proposer its `parent`, which is the whole of the self-reference. Plug-point stages 1–3 are its prerequisite.
3. **An archive-wide parent-selection member** with SIFT's `P(i) ∝ exp(−α·r_BT − β·r_acc − η·log(1+v))`, beside the acquisition [`parent-selection.md`](parent-selection.md) already wants. Their "unevaluated node inherits its parent's accuracy" lives inside this function and nowhere else — it may steer a choice, never become a measurement.
4. **A candidate-level pairwise judge + regularized Bradley-Terry.** New: `promptpotter/judges/` grades cells, not candidates. Full source of two candidates in, a preference out, bounded comparisons per new node, ranks out to members 3 and 5.
5. **Staged evaluation:** `elimination_rule: pobb | fixed_gate` (their small pass/fail gate as a literal replacement for PoBB), a priority queue by `r_BT + r_acc`, the full split on the chosen node.

Optional and later: expansion overlapping measurement ("disaggregated") — needed only to match wall clock, and it waits on § Run admission + concurrent serving.

**Models: cheap and already measured, not theirs.** S1 and S2 run on the dataset's own declared model; S3's agent is `qwen/qwen3.7-flash`; the candidate judge is `openai/gpt-oss-120b` throughout — the Harbor agent and the `sealqa` judge in [`../operations/dataset-reasoning-matrix.md`](../operations/dataset-reasoning-matrix.md). Whether to rerun on SIFT's own models is decided when the paper is written, not before.

**Benchmark.** Polyglot is not wired here; check Harbor's registry for an adapter before building gap 1's dataset.

**Declared inexact.** No code release: the judge prompt, the BT pseudo-count, the gate threshold and the queue's tie-breaks come from the paper or are chosen and stated. Their CPU-hours column is not reproducible — § Captured, CPU-hours.

**Proposed sequence — a suggestion, not a commitment. Risk goes last.** Gap 1 is the one that touches measurement identity and needs a new Harbor agent, so nothing waits on it.

- **S1 — SIFT's search on a surface we already have (the transition milestone).** Gaps 4, then 3 + 5 — no plug point, no code artifacts. The proposer stays `l1_generate`; the candidate stays a prompt on an already-wired **single-call dataset, not a Harbor one** (`justlogic-d234` is the cheapest, `bbeh` the paper's primary) — no container, no episode, nothing the SIFT members could be blamed on. Opens with the zero-build reading [`parent-selection.md`](parent-selection.md) § Open asks for: the candidate judge's ranking against realized θ over banked rounds, their ρ≈0.68 as the bar. *Closes when* one campaign runs end-to-end with the SIFT members selected, and SIFT-on-prompts vs SIFT-on-prompts + PoBB is served by `evidence` — the first two rungs of the ladder, which is a result on its own.
- **S2 — the proposer seam.** Plug-point stages 1–2, then gap 2. *Closes on* the plug point's own inertness check — a `proposer: ""` run writing a byte-identical `round_NNNN.json`.
- **S3 — code artifacts + Polyglot.** Gap 1, and only if S1's readings make the full reproduction worth it. Whether it runs through Harbor is open: Harbor may be more than a patched agent needs, but Polyglot grades by running each task's tests across several language toolchains, so SOMETHING must supply the sandbox — decide which when S3 opens, not before.

Within a milestone the gaps are independent worktrees, each closing on `scripts/gate.py` green: S1 is two (gap 4 as a standalone module with its tests; gaps 3 + 5 inside the round loop, against a stub rank until gap 4 merges).

### Probe rounds — what they mean, and why the lever is not wired

**This section stays at full length — it is a written SPEC, not stale description of an unwired lever.** It reads as prose about nothing because `L2ContextOutput` carries no `action` field, so a doc-shrink pass scores it as dead. Operator-decided: don't compress it, don't fold it into a code pointer, and don't file the missing field as drift.

**A probe round spends its whole budget interrogating ONE thing** — one axis, one variable, one recurring mistake — instead of spreading a broad mutation set across the failure surface. The distinguishing move is **more candidates on a narrower question**, so the round returns a real answer about that one thing rather than one noisy sample of it.

**L2 cannot request one today: there is no `action` field on `L2ContextOutput`, deliberately.** The shipped version selected samples by *warning* while the prompt asked L2 which *axis* to probe, and the warning inventory behind it was later deleted — leaving a set that only fills on backend degradation. On a healthy run it is empty, so the round scored every candidate 0/0 and persisted `accuracy: 0.0`, indistinguishable downstream from a genuinely terrible candidate, while still paying its optimizer calls and consuming a round. `l2_targets_l1_surface` even counted the choice as conformant — L2 scored 100% precisely by picking the action that measured nothing.

The lever was removed rather than guarded, because a guard leaves a no-op action sitting on L2's menu. **Re-running warned queries is degradation triage, not a search action**; conflating the two is what put an empty-set path in the candidate scorer. Build the real thing as defined above: pick the target from measurement (a peaked axis, a recurring failure cluster), widen `n_variants` for that round only, and select samples by *relevance to the hypothesis* — never by a set that is empty whenever the run is healthy.

### Origin-resolution check-in
Three invariants outlive the build: it reuses the `checkin/2` node (never a separate `origin_resolve` node or model); `reasoning_floor`/`ceiling` stay **off the operator surface**, backend-node-only; and model and provider are optimizer-locked. Concept: [`../architecture.md`](../architecture.md) § Origin, parent, and check-in. Post-flip the same node consults in RUN mode, raising `pause-cycle` / `change-run-limits` / `fork-cycle` instead of draft patches, over the `RaisedCommand` shape `application/datasets/origin_resolve.py` already carries. Lands with L4, not before.

### Ingest + chat-first web
> **Chat-first front door** (thread model, activity-stream translator, copilot decision buttons, campaign-scoped persistence) has its own contract: [`chat-foundation.md`](chat-foundation.md). This note keeps only the ingest / draft-campaign detail.

Four nouns map to OIDC: Install=`iss`, User=`sub` (`identity/user.py::derive_user_id` hashes the pair — not a concatenation, and the record carries no SCIM field name), Project=`tenant_id`, Campaign=cycle 1:1.

**The committed artifact is a Dataset, not a campaign:** 4 content-hashed files at `projects/{tenant}/datasets/{slug}/` (`cache.json` rows, `pipeline.yaml` overlay, `task_description.md`, `prompts/default.yaml`) compose into `JobSearchPoint.content_hash`; the sibling `campaign.json` is NOT in the hash. Identical datasets → identical `cycle_{target_hash[:12]}`, so two campaigns in ONE tenant pool their paid cells for free. **Cross-TENANT pooling is refused** — owned by [`../adr/0002-identity-foundation.md`](../adr/0002-identity-foundation.md) § Contract B; this lane must not build a feature that assumes it.

**The hash is dataset-scoped and pipeline resolution is campaign-scoped — owned by [`../architecture.md`](../architecture.md) § Two resolution seams**, which carries the census the decision was taken on. What this lane must not do is collapse them: the hash answers *are these the same measurement*, the resolution answers *what is this campaign doing*.

### Connectors + L4 inner-cycle execution
- **Connector contract** — owned by [`../../promptpotter/connectors/CLAUDE.md`](../../promptpotter/connectors/CLAUDE.md); a third party registers one through the `promptpotter.connectors` entry-point group, and no plugin may shadow a built-in.
- **Execution mode (the L4 self-recursion seam)** — owned by that same file; what is still open rides [`l4-outer-loop.md`](l4-outer-loop.md) § Open.
- **Parent selection** — collapse `elect_round_winner`'s greedy promotion and `select_rewind_round`'s UCB1 into one acquisition over the lineage tree, gated on a prior-vs-θ-rank correlation reading: [`parent-selection.md`](parent-selection.md). Not opportunistic for L4: selection must see lineage, not only the round's best ([`../research/external-constraints.md`](../research/external-constraints.md) § Ranked, item 2).
- **Composite fitness phases:** P2 per-candidate rollup + scatter · P3 `compile_post_aggregate_fitness(formula)` + `campaign.yaml::scoring_post_aggregate` · P4 Pareto-PoBB (stretch).
- **Prompt-injection Phase 2:** `TrustedText`/`UntrustedText` renderer types + L1/critique injection-echo validators + a repeat-detection circuit breaker.

### The connector as a unit — select, see its pipelines, pick a mode

**Draft: a direction and its constraints, not a design.** Still undesigned: the schema's fields, the connector panel's layout, the attachment's wire shape. Every bullet is operator direction, 2026-09-16.

**The operator picks a connector, not a backend URL.** At the origin each registered connector shows its live status and its **pipelines** (TermNorm: "LLM only", "Research + Match"; Harbor: "agent (terminus-2)"). A pipeline shows its **modes**; Harbor's skill delivery is *advanced* (the agent opens `SKILL.md`) or *primitive* (the skill sits in the system prompt, as in WikiSkill). Today the browser reaches only TermNorm's pipeline choice, through the check-in toggle. `pipeline.yaml::backend_type` fixes the connector, a CSV upload always drafts TermNorm, `ConnectorInspector`'s switch does nothing, and a verifier-graded dataset cannot be checked in from the web.

- **One declaration schema, so the connector is a platform entry point.** `Connector` (`connectors/protocol.py`) is the schema. `GET /connectors` serves its projection as a generated type, and the webapp draws every connector from that alone: label, pipelines, modes, node kinds, attachments, status. A new connector, built in or from an entry point, needs no webapp change. Backend-specific code stays in the adapter (wire shape, session, `extract_experiment`, `in_process_run`), and nothing branches on a connector's name; `connectors/CLAUDE.md` § Execution mode already holds that for transport. As backends converge on a common agent and tool protocol, the adapters shrink toward one generic adapter; until then, core stays agnostic. The schema absorbs the browser's current connector knowledge: `lib/origin-readiness.ts::CONNECTOR_LABELS` and `PipelineSetupSection`'s two preset labels.
- **The chat hero (`wf-hero`) is the connector's picture, nested connector ⊃ pipeline ⊃ node.** A node click opens its detail as today (model, parameters, starting prompt). A click inside the hero but off the pipeline opens the connector panel (status, pipelines, modes, current choice marked). With a campaign selected, the panel also shows what it runs, read from the same served resolution one level up. It is one element; where you click decides the level.
- **Advanced mode draws the skill as a tool attached to the agent, like an n8n sub-node.** It sits off the flow line, because the agent decides whether to open it. Its detail reuses the node parts (value lists, 🔒/🔓, prompt editor) and shows the body, which holds the evolved prompt fields, and the frontmatter, locked for the one-skill episode because it is never a search axis there — the skilled-agent target treats `description` as the routing surface ([`../research/external-constraints.md`](../research/external-constraints.md) § SKILL). The agent node keeps model, provider, host (`route_order`), `max_turns`, `temperature` and delivery mode. Primitive mode has no attachment: the body is the agent's starting prompt. The attachment is served, never guessed: when `Connector.prompt_delivery(params)` resolves to `artifact_body`, the pipeline view carries it and splits the config rows between agent and tool. `PipelineFlow` gains one geometry case, not a second graph, and later agent tools such as web search or code execution attach the same way.
- **The pipeline choice uses the existing channel.** The TermNorm toggle writes `DraftPatch.pipeline_steps` → `draft_active_steps` → `pipeline.yaml::pipelines.default` → `mint_and_start`'s `exclude_nodes` → TermNorm's wire `steps`. Only its two browser-side guesses go: the hardcoded "LLM only" and "Research + Match" read off the committed view. In their place, `Connector.default_pipeline` widens into a named set with one default (not a second field), which the draft and the pipeline view serve. The control moves into the connector panel with the same write, and the check-in section keeps the picture and the node detail.
- **A mode is a node-config key with declared options.** It is never a search axis (`never_axis`) and always part of measurement identity.
- **Served.** `GET /connectors` answers from `connectors.registered()`: name, execution kind, pipelines, their nodes, and each node's modes and options. Route and schema go into `api-openapi.yaml` before the handler. Status comes from `Connector.preflight`, bounded and cached, never from HTTP `/status`, which cannot see in-process connectors. `/backends` stays endpoint history, not a picker.
- **Check-in writes add no field to `EditDraftPatch`.** The connector goes through `connector`, the pipeline through `pipeline_steps`, a mode through `pipeline_overlay`. Two things are missing: validation against the declarations, with the node schema re-resolved on a change, and `draft_from_dataset` accepting a verifier-graded dataset, which has no labels.
- **A running campaign never switches in place.** A mode or connector change alters identity, so the dashboard switch opens a fork preview (`fork-cycle` with a seed overlay). The control stays operable, as I3 requires.
- **Order.**
  1. Harbor's second delivery mode, as a declared node-config key.
  2. `Connector` declarations (named pipelines, modes, node kinds) plus `GET /connectors`. Harbor's agent is not an `llm` node, and its skill is an attached tool.
  3. The TermNorm toggle moves into the connector panel and reads the served pipelines.
  4. The dashboard inspector reads the same object.
  5. The C5 write side uses the same overlays, so the declared but unwired `change-pipeline-param` is re-scoped or deleted, not built against the dataset file.

### Agent-tool parity — PromptPotter as a callable tool inside an operating agent

Two halves of one question, and they are siblings: parity widens how PromptPotter is **invoked**, § Application radius widens what it **emits**.

Today PromptPotter is driven by a human or by Claude via `/potter-run`. The next invocation surface is **parity as a first-class agent-callable tool**: an *operating agent* — the user's own, an agent harness like dsh, or an ML-research agent like NVIDIA's AutoResearch or verl — calls PromptPotter as one move in its toolbox. Those callers stack rather than compete: a harness is where an operator sits, verl and NeMo RL only start above the weights line, and a gateway sits below both. Parity means the MCP tool exposes the CLI/skill lifecycle — mint, run, supervise, read results — so an agent can operate a campaign end-to-end. Mechanism already on the board: **C5**.

**Deliberately held, not just unscheduled.** The REST API is the integration surface for now — protocol-agnostic, and callable by anything that can hold a browser session, which is the live constraint rather than a detail — **the inbound-credential gap is owned by [`../developer/stable-api.md`](../developer/stable-api.md) § 8. What is NOT stable**. MCP would need the same credential and would add a protocol on top of the gap. The protocol's shape is settled — [MCP 2026-07-28](https://modelcontextprotocol.io/specification/2026-07-28) with the Tasks extension, where a long-running call returns a task handle (`tasks/get` / `tasks/update` / `tasks/cancel`) rather than a polling or webhook scheme of our own ([`../research/external-constraints.md`](../research/external-constraints.md) § TOOL) — so the hold is the credential gap alone. Revisit once that closes and a concrete caller asks.

### Application radius — what PromptPotter EMITS, and the standing DSPy rule

**The standing rule governs every future integration, not just this one: whenever we plan an ecosystem expansion — a new integration, export, registry, tracing sink, or callable surface — DSPy is the reference we read before designing.** Not because their code is better; a source study says plainly that it is not. **Their ecosystem beats ours while their code does not**, and that asymmetry is the lesson: reach is won by being consumable, not by being well-built.

**M13 reverses the arrow without retiring the rule.** Being consumable stays the goal, but it now runs both ways: we install into DSPy as an optimizer, *and* DSPy's optimizers run inside us as proposers (§ The optimizer plug point). The second direction is the one that makes reach a *measurement* claim rather than a distribution one — a peer that runs here is a peer we can price. **Opik is the next host for the first direction, later rather than now:** its Agent Optimizer already sells six algorithms behind one API, GEPA among them, so PromptPotter registered there is one call away for a buyer who already pays for autonomous optimization ([`../research/landscape.md`](../research/landscape.md) § Langfuse and Opik).

**The gap, located.** PromptPotter today is *terminal* — it ends at "here is your prompt", and a human carries that to production. DSPy is *ambient*: it is already what runs, and optimization is one operation performed against it. The gap is not features but that our output is a **report** where theirs is an **artifact a running system consumes**.

**The boundary is not negotiable: we write a file and provide a reader. We never load, host, route, or hot-swap.** The swap belongs to the host — a registry alias, a path the app reads at boot, a committed artifact. Becoming a serving framework is permanently out of scope.

**The artifact** is `cycles/{id}/export.json`; its reader contract and the four rules it obeys are owned by [`../developer/stable-api.md`](../developer/stable-api.md) § 5c. Two of those rules invert findings in DSPy's source: `Signature.dump_state` writes fields positionally and `load_state` zips them back with `strict=False`, so a signature that gained a field reloads a scrambled prompt with no error at all — hence **field names, never positions**; and their artifact carries no provenance, which is the half we compute and they do not.

**The radius stops at the model the winner was won on.** [Why Prompt Optimization Works, and Why It Sometimes Doesn't](https://arxiv.org/abs/2605.26655) found optimizer edits that help one benchmark often fail on another across four model families, and [Prompting Inversion](https://arxiv.org/abs/2510.22251) reports a scaffold that helps GPT-4o and hurts GPT-5. So an export names its model, and a model swap is a new run, never a copy of the winner; the pitch leads with mid-tier and open-weight serving models, where gains do not shrink or invert the way they do on the strongest reasoning models.

**Open — two of the three consumers.** Our own runtime is built. `to_dspy` lives in the `promptpotteropt` repo, never here (the dependency arrow is one-way — [`ADR-0006`](../adr/0006-embeddable-core-and-extras.md)), waits on Phase C, and applies the winner to a *live* program rather than emitting DSPy state, sidestepping the positional-zip corruption. MLflow is unwritten and targets the **Prompt Registry** (`register_prompt` / `load_prompt` / `search_prompts`), **not a model flavor**, because we produce prompts, not programs. Its registry semantics (placeholders, versions, aliases) bind the write-back: [`../research/external-constraints.md`](../research/external-constraints.md) § TOOL.

### Ecosystem outreach — optional, unscheduled, no commitment implied

Places PromptPotter could get *found* by systems it complements. None are promises; the list exists so it is not lost, tiered by what blocks each.

- **Nothing blocking — DSPy.** `PromptPotterOpt` already exists and installs ([`dspy-optimizer.md`](../developer/dspy-optimizer.md)), so this is a real integration rather than a pitch, and the one item here worth not letting sit. A PR/listing pointing DSPy users at it, alongside `dspy.GEPA`, is next up.
- **No blocker, unsequenced — verl and NVIDIA AutoResearch / Karpathy's `autoresearch`.** Doc-level PRs making the harness-vs-weights case: try a frozen-model harness tune before spending on a training run. The case says *before*, never *instead of*: [GEPA](https://arxiv.org/abs/2507.19457) beats GRPO by 6% on average with up to 35× fewer rollouts, and [Databricks](https://www.databricks.com/blog/building-state-art-enterprise-agents-90x-cheaper-automated-prompt-optimization) found SFT and GEPA compound (+4.8 together on GPT-4.1). For AutoResearch a *skill* entry is stronger than a doc PR, since their loop already runs on markdown skills, and it does not need C5.
- **No blocker, a capability to build — bidirectional MLflow/Langfuse/LangSmith.** Today's sink is fan-out only, so the optimizer never reads it and it can never become load-bearing for the loop. **Write-back** pushes the winning prompt to MLflow's Prompt Registry or Langfuse's Prompt Management, so a team's production app picks up the winner with no PromptPotter dependency at request time — the existing Connector/sink shape, no new search mechanism, mapped onto each registry's own version and label semantics ([`../research/external-constraints.md`](../research/external-constraints.md) § TOOL). **Read-in** ingests a team's traces as a dataset source, chipping at the labeled-dataset requirement, but production traces carry no guaranteed labels, plus PII and format drift, so it needs real design rather than a flag. A LangSmith **Dataset** is the exception — labeled and split already — so it can enter as a "pull from LangSmith" option at the origin check-in's dataset step, landing on the same `Table` as `csv_ingest.py`. **Executing** there is narrower than it looks: a Hub prompt cannot be run remotely (their REST evaluation API has the caller run the model), so a remote LangSmith connector targets a **LangSmith Deployment**, with the candidate injected through `config.configurable`.
- **Waits on C5 — OmniRoute and DeepSeek Harness.** OmniRoute is the gateway-pairing case, untried, and earns adoption only where evaluation spend is the binding constraint; its live router must never act on the evaluation path ([`../operations/backend-integration.md`](../operations/backend-integration.md) § Endpoints), because optimizing a routing *policy* is an axis while routing during a campaign voids the numbers. DeepSeek Harness is the operating-agent category itself, and its ecosystem already names MCP servers as a first-class submission category — likely the lowest-friction placement once C5 lands.

### Judged and turn-structured scoring

**Tracked as C2b, and most of it has SHIPPED.** The judges, the grading call path (reuse cache, 429 retry, heartbeat, metering, all decided once at `judges/call.py::ask`), plural judges keyed by the term the formula reads, and the `retrieve → ground → answer` step schema are all live; `promptpotter/judges/CLAUDE.md` owns every contract and reason behind them. It deliberately does NOT live in `pipeline.yaml::nodes` — `node_config_items` is the walk a bulk model steer follows, and that is exactly the boundary a judge may not sit behind.

It unblocks datasets no matcher can grade, and the first is wired and running: `datasets/sealqa-longseal-12/`. Two more record being stuck on it — `email-tagging`'s free-text CRM fields and `screen-taste-v0`'s rating.

**The conversation reaches a formula, and only as scalars.** `domain/scoring.py::turn_scalars` projects `n_turns`, `n_tool_calls` and `{step}_turns` at measure time, beside the `{step}_{reward}` terms the harbor connector banks. The raw `turns` list stays unreachable from a formula on purpose and must remain so: the compiler's AST allowlist has no subscript, no attribute access and no `len`, and `turns` is the first key `compact-archive` moves out of a cold row, so a formula that walked it would raise on every cell it had already scored. Two routes reach scoring from a conversation and there is no third — this projection, or a judge that reads the turns and banks a term.

**What remains, at three distances.** *Per-step difficulty* is the larger prize and the only part needing a run rather than a decision; its blockers and its one non-skippable precondition — bank each step's term separately, which the shipped schema now does — are at [`../methods/verdict-resolution.md`](../methods/verdict-resolution.md) § Phase 3. The two authored rubrics have since been screened on the `def880` campaign and BOTH were removed: they saturated and graded a property of the cell rather than of the arm, so they could not separate arms however many paid for them (`datasets/sealqa-longseal-12/campaign.yaml::judges` records the numbers). The criterion that screen established — **does the term vary across arms on the same cell** — is what any future rubric owes before a campaign funds it. *Scoring* one step is already config and step-agnostic. *Optimizing* one step is near: the candidate prompt is trial-scoped (one `SKILL.md` per episode), so an arm mutates the whole episode however narrowly it is scored, and the open design is whether a declared step becomes a NODE — one per step with its own `prompt_info` and `param_keys` — which makes the existing `campaign_config.exclude_nodes` the step selector and adds no new channel. It costs generalizing `connectors/harbor.py`'s singular `AGENT_NODE`. Separately: not RUNNING the later steps, where the cost saving lives, since Harbor runs every declared step and `_unscoreable_step` raises on a truncated one.

**PARTIAL PIPELINE GENERATION — far, and deliberately so.** Today L1 always proposes a COMPLETE pipeline. The feature is that it may propose a partial one, with *which* subset being **L2's** decision — the first place the escalation layer chooses a search SHAPE rather than a search direction.

- **Do not open it until a real pipeline is blocked without it.** With few steps a whole-pipeline proposal is both cheaper to reason about and strictly more expressive. The gain appears only where a pipeline has many steps and no way to tell which one moved the number.
- **Its trigger is a FORK, not a stall.** The signal is L2 reaching a two-way split it cannot choose between: two strategies, no evidence favouring either, and a whole-pipeline arm that would confound them. A plain stall belongs to the existing escalation ladder.
- **It is a human-in-the-loop design cycle, not an implementation ticket.** What a partial proposal may touch, how a partial arm compares against a whole one, and what a δ ruler does with arms of different shapes are all open and none is answerable from code.

**SHIPPED, and the residual it asked for is measured.** A grading that failed past its retry left `rescore_results` raising inside `measure_sample`, and the catch-all banked the cell as an ERROR — discarding a backend answer already paid for, and tripping `query_loop.py::_abort_reason` on `PIPELINE` so the candidate's whole remaining walk went with it. The residual, asked for here before acting: **zero banked cells**, because no dataset's formula names a judge term yet — recount with `compact-archive inventory` before quoting a denominator. `formula/rescore.py` now resolves such a row to UNSCORED — measurement kept, both verdict keys absent, out of every denominator — and a formula that RAISES still halts loud, since that one is a contract bug every cell fails.

### Selection-clean reporting
**Why, and the statistical statement, are owned by [`../research/benchmarks.md`](../research/benchmarks.md) § The winner's own number is biased upward.** What this lane owes: a reserved per-dataset partition the loop never scores on, and two readers pointed at it — `verify` (which already re-scores a frozen candidate, C0 included, without touching the cycle, so it is the closest existing shape) and the reported fitness in `export.json`, whose provenance block advertises a deployment estimate it cannot currently claim. Sequenced with the publication lane, not before it: an in-sample headline is wrong in a direction that flatters us, so it costs credibility at publication rather than correctness in the loop. The published BBEH comparison is not what this fixes — its split already satisfies the requirement.

### Evolving agent harnesses
**Tracked as C7, ahead of v1, and the connector half has SHIPPED** — `connectors/harbor.py` plus `datasets/harbor-tbench-regex-log/`. The artifact to evolve becomes an **agent harness** — an agent's prompts, tools and control flow — rather than only a single LLM call or a declared pipeline. Wording alone is the smaller lever on agentic tasks: LangChain's deepagents went from 52.8% to 66.5% on Terminal-Bench 2.0 with the *same* model through prompt, tool and middleware changes ([harness engineering](https://www.langchain.com/blog/improving-deep-agents-with-harness-engineering)).

**Do not re-open "whose harness — dsh or LangChain".** That question binds the design to one vendor's harness and inherits its mutation surface. [Harbor](https://github.com/harbor-framework/harbor) sits a layer below — evaluation infrastructure, in `pytest`'s category — and drives ~30 agents behind one `BaseAgent` interface, so which harness is under test is one line in a dataset's `harbor_tasks.yaml` rather than a design commitment.

Two things in the remaining gap are real rather than cosmetic, and both shipped: a **labelless cell** (`Sample.ground_truth is None`, since a verifier grades where no label exists) and an **arm-time observation-key contract** (`Connector.required_observation_keys`). The **per-cell wall clock** L4 solved privately is now `Connector.cell_envelope_s`, enforced for every backend at the one seam a cell is measured. The per-cell **spend** bound is built too: a harbor cell reserves what one episode bills as declared (`connectors/harbor.py::_sent_spend_bound`), and every send the agent makes is admitted at its own bound (`infrastructure/llm/litellm_sends.py`). What stays open is harbor's **per-cell wall clock** — it declares no `cell_envelope_s` yet, so only Harbor's own `task.toml` bounds an episode's time. A cap on measured counts is not a substitute: tokens and dollars jitter run to run, so it would cut the same cell on one run and not the next.

SkillOpt, DarwinX and AutoDesign already evolve harnesses for a frozen model, and DarwinX states our own one-armed-search argument back at us. What that comparison leaves standing is what a PromptPotter version must keep rather than re-derive — sequential elimination, cost-per-fitness, subset-invariant ability. Their benchmarks are agent environments `harbor` can now measure but PoBB's cost model still cannot bound, so adopting the target does **not** mean adopting their evaluation suite.

**Under M13 they stop being only a comparison and become candidate ARMS.** The WikiSkill / SkillOpt *setting* is already here as Harbor's advanced-vs-primitive skill delivery, so what the plug point adds is their *method* beside ours on the same cells — which is what answers "are their methods actually better at the same model", a question the published numbers cannot settle because nobody held the model constant. The factorial follows for free: proposer × delivery mode, read by `evidence --grid`.

**The target worth building toward is a skilled agent.** That means one agent equipped with many skills, where each skill is maintained, evaluated and updated separately, rather than one monolithic prompt tuned as a whole. The design is ours to build: it is one configuration of our agent toolkit, and we optimize each skill as well as evaluate it. [NVIDIA SkillEvaluator](https://github.com/NVIDIA/skillevaluator) covers only the evaluation half of that one design, and it is not a competitor ([`../research/landscape.md`](../research/landscape.md) § NVIDIA SkillEvaluator). Adopting and testing it is for later. Its three tiers — **validation** (is it safe and well-formed?), **deduplication** (does it repeat guidance?) and **live evaluation** (does it help the agent, measured against not having it?) — are a search *strategy*, so the part our infrastructure already carries enters as **L2 and L3 context**, not as new machinery. Measured lift is the only admission ticket: [SkillsBench](https://arxiv.org/abs/2602.12670) found curated skills gain +16.2 pp on average yet hurt 16 of 84 tasks, and self-generated skills gain **zero** — so an LLM's rewrite alone never admits a skill, and SkillsBench is the external harness to report skill results on. Whether the optimizer should itself become a skilled agent rather than the fixed L1–L3 loop is the same kind of question, and an empirical one: [AFlow](https://arxiv.org/abs/2410.10762) beat hand-designed workflows by 5.7%, [AI Scientist v2](https://github.com/SakanaAI/AI-Scientist-v2) did not reliably beat its templated v1, and [EvoAgentX](https://arxiv.org/abs/2507.03616) keeps the search algorithm apart from the workflow shape so both can be tried on the same cells.

**Now, on what exists.** The tiers live in the shared optimizer prompts (`assets/optimizer/pipeline.yaml`): when the candidate is an agent skill, `l2_context/1` checks the tiers in order and names the first failing one in `axis_targeted`, and `l3_plan/1` replans onto it; every other target reads those prompts unchanged. The one unbuilt piece is tier 1's size rule, which is code.
- **Tier 2 → no repeated guidance.** A skill grows by appending, and appended guidance overlaps what the skill already says until two lines disagree. So the prompts say: prefer merging or deleting a line over adding one, and never restate guidance the skill already carries. `mutation_memory` already shows L1 what was tried, so the repeat is visible to it. SkillsBench found focused skills of 2–3 modules beat comprehensive documents, and [Trace2Skill](https://arxiv.org/abs/2603.25158) is the proposer pattern to prefer over a free rewrite: it patches from success and failure traces and merges them into one conflict-free skill.
- **Tier 3 → the ablation move, and a no-skill anchor.** The prompts say a line that cannot beat its own absence is removed rather than reworded, which makes deletion a first-class mutation. The baseline is measurable today: an origin with an empty `SKILL.md` is an ordinary campaign, and the matched-parent lift on the same cells prices every later skill against it. What does not exist is the no-skill arm running *beside* the search inside one cycle; that waits on M13's arms.
- **Tier 1 → a strict validator, the one piece that is code.** The candidate is the skill's body; `connectors/harbor.py::_write_skill` owns the frontmatter, so a mutation cannot break the header. What a mutation *can* break is the [Agent Skills spec](https://agentskills.io/specification)'s size bound, a body under 500 lines ([`../research/external-constraints.md`](../research/external-constraints.md) § ranked 5). A `validators/l1_strict.py` rule would reject an oversized body before it runs, at $0; it is unbuilt, so today only the L2 prompt names the bound. Security review and dependency checks stay with SkillEvaluator, but tier 1 also holds a security floor no mutated body may cross — no Unicode Tag characters, no instruction to read secrets or environment variables, no outbound calls or exec/eval — the attacks [SkillSpector](https://github.com/nvidia/skillspector) (71 patterns; a risk score above 50 means do not install) and [Cisco skill-scanner](https://github.com/cisco-ai-defense/skill-scanner) already scan for, and a SkillSpector-class scan before a candidate is measured is the long-run form. The skilled agent moves `description` into the search, and the frontmatter rules then join this check.

**Before the library grows — skill routing.** Selection degrades as the library grows (measured at 52, 102 and 202 skills, up to 21% worse, with no safe threshold given), from wrong-skill picks rather than from context ([More Skills, Worse Agents?](https://arxiv.org/abs/2605.24050); [`../research/external-constraints.md`](../research/external-constraints.md) § SKILL). The skilled agent needs its routing design before its library reaches a few dozen skills, not after, and overlapping descriptions are what seed the wrong picks, so mutual distinguishability against sibling skills becomes an objective; [SkillRouter](https://arxiv.org/abs/2603.22455) shows retrieval at library scale is embedding-based and also reads skill bodies. Prose skills themselves are the open question: in [SkillWeaver](https://arxiv.org/abs/2504.07079) skills written as callable APIs transferred from a strong agent to a weak one (+54% on WebArena), and ours are prose.

**Later, on SkillEvaluator.** Semantic overlap measured against the whole skill library rather than asked for in the framing, its four-bucket synthetic datasets, and Skill Lift as a served statistic beside ours.

The unit of optimization then becomes the skill and the unit of acceptance becomes the library. Evolving one skill is re-measured against the others it ships with, which is where tier 2 and tier 3 meet.

### Schema axes — the one open step
Why the schema steers at all is [`../concepts/structured-output.md`](../concepts/structured-output.md). Two axes now ride the same gate: the `description` prose, and whether the node uses its schema at all (`response_format`). **Open: contrast them on `justlogic-d234`** — promote at `proxy_lift_corr ≥ 0.6`, and a negative result closes an axis by reverting it. The verb is already there: `evidence --grid ROW,COL` DISCOVERS its factors off each subject's own config (`evidence/grid.py::levels_by_subject`), so a varying `response_format` lands as a config factor and a varying schema `description` as a prompt-field one. What is missing is the paired run, not a surface to read it on.

### Fitness comparability — the slice-4 remainder
Open: the **cross-round headline surfaces** + the lineage `/N` badge, and **feeding graduated discrimination `aₛ` into `select_round_subset`**, which is still 1PL ([`../methods/verdict-resolution.md`](../methods/verdict-resolution.md)).

### Exit gate
`rounds_to_separable ≤ 5` on `llm_only` AND TermNorm under the same `l1_generate_hash`; `behavior_pass_rate = 1.0` seeded; `proxy_lift_corr ≥ 0.6` over ≥4 paired branches (or modify the rules). **The clock is separability, never an absolute accuracy** — the admission bar takes datasets scoring 15–40% at origin, so a fixed accuracy target sits above their ceiling by construction and closes on nothing.

### Host coupon + BYO per-user API keys
**The whole mechanism is owned by [`ADR-0003`](../adr/0003-spend-and-tenancy.md) § Host coupon + BYO keys** — `grant.json`, `key_source`, the three-step resolution order and its two 422s, `TenantApiKeyStore`, and the auth-router verbs. In one line: the host runs users on its own keys up to a per-user coupon; past it a user uploads their own key and continues on their own money. The coupon protects the host wallet, `jobs/quota.py` protects the machine, and the two stay separate.

**Status: none of the coupon exists in code today** — no `HOST_ALLOWANCE` member on `StopReason`, no `grant.json`, no `TenantApiKeyStore`; `key_source` is declared in the asyncapi only.

### Anonymous preview tier
Spec only — **do not build it before the chat backend exists** (`presentation/api/routers/chat.py` is absent), because an anonymous meter with no conversation to meter is a writer with no reader.

- **Its own issuer sentinel, and `is_anonymous` asked BEFORE the operator arm.** This is the trap, not a detail: `quota.py::spends_the_hosts_own_key` reads a missing issuer as "came through the terminal, which only the operator reaches", so an anonymous identity built by leaving the issuer unset resolves as the box operator and is metered by nothing. The sentinel makes the two distinguishable; the ordering makes the distinction bind.
- **Empty capability set.** No command verb, no launch, no ingest, no dataset write — it converses and reads, and every action surface is a sign-in prompt. Nothing is added to `CAMPAIGN_CAP_BY_NAME`, so `_require_capability_for` refuses each verb with no per-verb exception to keep in step.
- **One shared tenant.** `UserStore.get_or_create` writes a `user.json` per tenant, so a tenant per visitor is an unbounded directory-creation surface reachable without authentication. One tenant gives the spend a reader for free: a single row in `jobs/install_spend.py::read_install_spend`.
- **A global daily pool is the ceiling; the per-visitor allowance is UX.** A per-visitor cap is defeated by discarding the visitor, so the only figure that binds is one install-wide daily pool. `admit_launch`'s reservation does not apply — an anonymous turn is one call, and anonymous cannot launch a campaign at all.
- **No trustworthy origin signal ⇒ refuse the request**, or the pool becomes a scrape budget assembled from millions of small turns.

### Operator-steered fork
Two rules survive the build. `config_overrides` carries the fork's whole `OptimizationConfig` delta — run limits **plus** the policy toggles — so **a fork at offset 0 A/Bs a behaviour knob in isolation** without moving the global default; that is the standing "behaviour-knob change → sibling cycle" workflow. And `max_rounds` on a fork is an absolute target, not a remainder: the fork's counter continues from its parent.

### State-sync
**Teardown-only design was rejected — do not re-propose** (it reverses the folder-UI §0 commitment). The two state surfaces it concerns are owned by [`../operations/persistence-and-state.md`](../operations/persistence-and-state.md).

### Run admission + concurrent serving
Nothing is open in the mechanism. Campaigns run concurrently up to a resolved capacity, a full box QUEUES rather than refuses, and the queue drains least-served-first; every entry point takes the same slot from the same line, the terminal included. The substrate is an OS file lock, so admission is atomic across processes and a dead producer's slot returns at once — which is also C6's predecessor: `--workers > 1` and a containerised worker need no second mechanism. Owned by [`../operations/access-model.md`](../operations/access-model.md) § What bounds resource use.

- **Open — full-speed-each needs more provider quota, not a better divider.** Two busy users on one key each get roughly half the throughput however the window is shared; only **Lane A2** buys more. It is cheap once reached: an `api_key` dimension on `get_llm_client`'s cache key gives every tenant its own limiter.
- **Distinct axis — sample look-ahead *within* one run.** `capacity` is how many CAMPAIGNS the box admits; look-ahead is how many CALLS one scoring round holds in flight, so it does not cross-bill and is gated separately on `campaign.lookahead`. **Open:** the ceiling is a hand-set constant per connector rather than measured peak-RSS headroom (there is an OOM post-mortem in `runner/inner/spawn.py`), and inner and outer still share the process-global `RateLimiter` with no per-tenant slice. Only TermNorm's is measured, and only on `llm_only` — its web-search pipeline meters one Brave query per cell against a per-key rate limit, unprobed at that depth; `harbor`, `dspy` and the recursion keep guesses. Re-test: `seed-screen --parallel N` at rising N on a dataset of that backend, reading wall clock and error rows per depth. **Open, forward:** the horizon is a worst case — a cell is launched only if no rule can POSSIBLY cut before it — so early in a walk PoBB's reachable-but-unlikely cut holds look-ahead near `n_min`. A horizon priced in expected waste (P(cut before i) × a call) would launch deeper at a bounded expected cost. Boundary: [`../operations/access-model.md`](../operations/access-model.md) § host-admin ↔ user.

### Lineage mask
The read side is [`../operations/mask-projection.md`](../operations/mask-projection.md); code SoT `application/mask/`. The **write side — fork-from-divergence — shipped**: `fork-cycle` carries `keep_rounds`, which swaps the offshoot trigger for `OPERATOR_REWIND` so rounds `0..N-1` are lifted, and `ConfigOverrides.scoring` carries the criterion into the fork's effective config. The alternative branch still materialises only as a real, measured fork the operator chose to run — never a stored forecast tail — which is why persisted mask identity is still not a thing: the criterion rides the seed the fork already writes.

Open as **Lane C8**: the SAMPLE-SET half, and it is a different substrate rather than a second knob. A scoring mask forks on the existing rails because it re-selects a *candidate* from arms the run measured; a mask that changes which samples a round buys needs a replayable measurement ORDER seeded into the fork, which `CycleSeed` does not carry and `ForkSpec` does not root at. Until it does, a sample-subset mask previews and stops there.

### Rebase forfeit — pricing WHERE L2 rewinds to
UCB1 over normalized Rasch θ decides where a rebase fork re-expands from (`mask/backprop.py::select_rewind_round`) with **no cost term at all**, so an ancestor deep in the tree looks free. It is not — and the operator's own framing of the loss, "go back to the start and you lose all the data", is wrong in the half easiest to believe: measurements are content-addressed by `(node_configs, sample_id)`, so everything already measured replays free at any cut. What a rebase actually forfeits is (1) **search position** — the θ climbed between the target and the frontier, which mutations from the older parent must re-earn; (2) **evidence leverage** — visits above the cut stop informing the new branch; (3) **future cache misses** — candidates minted from an older parent are new content hashes, and the replay fraction is predictable from observed reuse at comparable cuts (`LineageNode.cached_samples`). Against all three sits the countervailing gain: a target whose adjacent child edges differ by ONE data-affecting knob (`application/knobs.py::classify_config_diff`) buys a controlled one-parameter contrast worth more than its raw θ.

Shape: a third pure fold beside `divergence.py` and `scenario.py`, over the same `SpineCycle` list UCB already loads, emitting a per-candidate-cut `ForfeitReading`. **Served diagnostic FIRST** — stamped on the tree's course nodes so the operator sees the price of each WHERE — and only then folded into the acquisition as `Q − λ·forfeit`, λ a settings constant beside `UCB_EXPLORATION_C`. It never reaches L2 as prose: WHETHER to fork is the LLM's call and WHERE stays UCB's arithmetic, so no panel enumerates ancestors.

### Plus-backlog (opportunistic, unscheduled)
Hard-Sample Sorter Phase 2/3 · Webapp perf: SSE client cutover for the **dashboard** (the *chat* already consumes `events:subscribe` via `useCycleEvents`; the dashboard still 2 s-polls), SWR/TanStack, strip redundant memos under React Compiler · MCP server mode · research extensions.

## Captured — pending triage

- **Origin panel that drifts on the δ ruler** — the panel every green bar is read on is the first `sp_budget_round` cells C0 answered, fixed for the life of the cycle (`domain/results.py::origin_panel`). The far-out version lets it swap a cell once the ruler has settled, drifting far slower than the acquisition subset the winners are decided on. Zero priority: the fixed panel is what makes the bars comparable, and drift can only cost that.
- **Export / copy from dashboard** — one-click copy of the winning prompt + state on the optimizer box. The artifact is § Application radius's; this is it behind a button.
- **Origin check-in plain-language recap** — folded into the origin check-in flow; pending review.
- **CPU-hours as a suite-wide measurement** — container and CPU time metered per phase (expansion · judge · evaluation) beside `SpendRollup`, for every connector rather than for one comparison. SIFT's cost table (§ SIFT reproduction) is the shape; to be designed, not dropped.

## Identity — live forward gap (non-derivable)

Identity is at **Stage 1** by [`ADR-0002`](../adr/0002-identity-foundation.md)'s own ladder, which has three rungs and no 0.5: Stage 1 IS "OIDC client, same file-based layout, tenant-prefixed for real", and that is what ships. Tenant isolation is enforced — structurally, by `build_stores` ([`../operations/access-model.md`](../operations/access-model.md) § user ↔ user — tenancy). PostgreSQL + RLS is **Stage 2**, so its absence is the definition of Stage 1 rather than a shortfall from it; there is no SQL driver and no SCIM vocabulary anywhere in the package. What is genuinely owed is ADR-0002's no-drift gate #6 (SCIM field names on the internal `User`), unimplemented.

## Non-functional requirements

| Requirement | Target |
|---|---|
| LLM providers | OpenAI-compatible (OpenRouter default) |
| Python | 3.13 |
| Crash recovery | append-only `measurements/runs/{run_id}.jsonl` + the per-cycle ledger; resume cache-hits prior |

**No wall-clock or per-campaign storage target is declared, and neither should be re-added.** Both
were, at per-item rates set before an agent episode was a cell. What a cell costs is measured in
[`../operations/dataset-selection-rationale.md`](../operations/dataset-selection-rationale.md)
§ What a cell costs, and what a workspace holds on disk is `compact-archive inventory`'s — the
archive is not scoped by campaign, so a per-campaign figure is not a question it answers.
