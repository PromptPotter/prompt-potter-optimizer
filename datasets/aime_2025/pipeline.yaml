name: AIME 2025
version: v0.1
description: >-
  Competition math reasoning — prompt optimization via TermNorm /matches with the llm_only pipeline.
backend_name: TermNorm
backend_type: termnorm
available_models:
  - openai/gpt-oss-20b:nitro
  - mistralai/mistral-small-3.2-24b-instruct
  - openai/gpt-oss-120b
nodes:
  llm_only:
    type: llm
    runtime: backend
    node_role: ranker
    description: >-
      Direct LLM generation on the TermNorm backend. `max_tokens` is raised in campaign.yaml (see
      dataset.md).
    config:
      provider: openrouter
      model: openai/gpt-oss-20b:nitro
      temperature: 0.7
    optimizer:
      param_keys:
        - temperature
        - max_tokens
        - model
        - reasoning_effort
        - persona
        - task_intent
        - problem_description
        - instruction
        - thinking_style
        - answer_format
      observation_name: llm_only
      observation_mappings:
        - pipeline_key: final_ranking
          output_field: generated_text
          is_llm: true
pipelines:
  default:
    - llm_only
