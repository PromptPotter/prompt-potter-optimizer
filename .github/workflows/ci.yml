name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Lint
        run: ruff check api/ tests/

      - name: Format check
        run: ruff format --check api/ tests/

      - name: Type check
        run: mypy api/

      - name: Tests
        run: pytest tests/ -q --tb=short --cov=api --cov-report=term-missing
