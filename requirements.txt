#
# Mirrors pyproject.toml [project.dependencies] (core only — no extras).
# For development: pip install -e ".[all,dev]"
# For Docker/deployment: pip install -r requirements.txt
#

# Core FastAPI
fastapi>=0.109.0
scalar-fastapi>=1.0.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.3
pydantic-settings>=2.1.0

# LLM Providers (core). Anthropic is optional — see [anthropic] extras.
# Groq + OpenRouter route through the OpenAI-compatible client (no SDK).
openai>=1.10.0

# Utilities
python-dotenv>=1.0.0

# HTTP client
httpx>=0.26.0

# Locking
filelock>=3.12.0
