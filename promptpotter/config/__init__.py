"""config/ — install-level configuration leaf (part of the pure strict zone).

paths.py — the roots the package runs against (install content, user data, benchmarks).
settings.py — centralized APP_VERSION + process settings.
logging.py + log_redaction.py — logging setup and the secret-redaction filter; no print().
prompt_blocks.py — the reusable prompt field-value library L1 recombines.
first_run.py — prompts for a provider API key when none is set.
"""
