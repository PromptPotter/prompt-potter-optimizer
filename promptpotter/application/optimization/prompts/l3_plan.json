{
  "persona": "You are an expert optimization strategist.",
  "task_intent": "Both the inner prompt generation loop (L1) and the parameter tuning loop (L2) have stalled.",
  "problem_description": "L2 ADJUSTMENT HISTORY:\n{{l2_summary}}\n\nCURRENT PROMPT:\n---\n{{rendered_prompt}}\n---\n\n{{pipeline_section}}{{runtime_failures_section}}{{l2_output_failures_section}}\n\n{{axes_digest}}",
  "instruction": "The current approach is not working. Suggest a fundamentally different optimization strategy. If a runtime failure trail is present above, treat those patterns as discovered constraints on the search space — L2 has already tried to self-heal around them and failed, so your replan must either change pipeline_params to escape the failing region (switch model, raise a param floor, swap a node) or change the plan text to steer L1/L2 around it. Do not propose a plan that re-enters the same failure mode. If an L2 OUTPUT FAILURES block is present, deterministic validators caught L2 producing broken output (duplication, self-repeat, catalogue-redundant overrides) — L3 is L2's nurse here: refine the plan text to steer L2 toward novel mutations and away from the failing strategy mode.",
  "thinking_style": "Consider:\n- What patterns in the L2 history suggest about why the current approach is failing\n- What fundamentally different strategies the data supports\n- Which pipeline dimensions have been under-explored\n- Which runtime constraints have been discovered and must be respected\n- Whether L2 itself is the problem (validator failures on L2's output) and the plan must coach L2's next pass",
  "plan": "{{current_plan}}",
  "answer_format": "\nReturn a JSON object with:\n  \"plan\": new strategy text for guiding future optimization\n  \"pipeline_params\": {\"step_name\": {\"param\": value}} (or {} for no changes)\n  \"rationale\": 1-2 sentence explanation of the strategic shift"
}
