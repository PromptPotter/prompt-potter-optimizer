"""HTTP read API — the routers ``main.py`` mounts at ``/api/v1``.

Nothing is re-exported here; the surface map is ``presentation/CLAUDE.md`` and the
declared wire contract is ``docs/specs/m12-api-openapi.yaml``.
"""
