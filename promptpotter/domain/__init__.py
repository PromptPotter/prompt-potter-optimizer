"""domain/ — the pure layer: frozen Pydantic models + pure types, no I/O.

Backbone primitives, the sanctioned ``deprecated`` vocabulary and the
StrictModel rule: ``promptpotter/domain/CLAUDE.md``.
"""
